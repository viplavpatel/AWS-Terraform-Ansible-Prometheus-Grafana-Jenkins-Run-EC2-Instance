---
- name: Test for Grafana and Prometheus
  hosts: main
  gather_facts: no
  tasks:
    - name: Grafana Test
      ansible.builtin.uri:
        url: http://{{ inventory_hostname }}:3000
        follow_redirects: none
        status_code: [302]
   

        
